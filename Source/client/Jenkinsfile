pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                dir('Source/client') {
                    sh '''
                        pwd
                        npm install
                        npm run build
                    '''
                }
            }
        }
        // stage('Test') {
        //     steps {
        //         sh 'npm test'
        //     }
        // }
        // stage('Deploy') {
        //     steps {
        //         sh 'npm run deploy'
        //     }
        // }
    }
}
